version: '3.8'

services:
  stalwart:
    image: stalwartlabs/mail-server:latest
    restart: unless-stopped
    ports:
      - "25:25"
      - "465:465"
      - "587:587"
      - "143:143"
      - "993:993"
      - "80:80"
      - "443:443"
      - "4190:4190"
    volumes:
      - stalwart-data:/opt/stalwart/data
    environment:
      - STALWART_ADMIN_EMAIL=postmaster@mg.cargoffer.com
      - STALWART_ADMIN_PASSWORD=${STALWART_ADMIN_PASSWORD:?variable required}
      - STALWART_HOSTNAME=mail.mg.cargoffer.com
    networks:
      - mail

networks:
  mail:
    driver: bridge

volumes:
  stalwart-data:
